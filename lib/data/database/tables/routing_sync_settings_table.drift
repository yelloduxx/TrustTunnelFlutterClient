CREATE TABLE IF NOT EXISTS routing_sync_settings(
  id INTEGER PRIMARY KEY NOT NULL,
  enabled BOOLEAN NOT NULL DEFAULT true,
  interval_minutes INTEGER NOT NULL DEFAULT 30,
  updated_at TEXT NOT NULL
);
